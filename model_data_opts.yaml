cohorts:
    tbl: urm_or_disadvantaged
    col: urm
    included:
        - not urm

# list columns to exclude from table, by default include all
features:
    - experiences
    - grades
    - mcat

outcomes:
    screening

predictions:
    screen_eligible

algorithm_name:
    debug
